terraform {
  required_providers {
    virtualbox = {
      source  = "terra-farm/virtualbox"
      version = "0.2.2-alpha.1"
    }
  }
}

provider "virtualbox" {}

resource "virtualbox_vm" "example" {
  name   = "vm_terraform"
  image  = "/home/ubuntu/Downloads/iso/ubuntu-24.04.1-live-server-amd64.iso"  # Ruta al archivo ISO o imagen de máquina virtual
  cpus   = 2
  memory = 1024
  network_adapter {
    type = "bridged"
  }
  disk {
    size = 10240  # Tamaño en MB
  }
}
